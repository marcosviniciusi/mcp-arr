apiVersion: v1
kind: Service
metadata:
  name: mcp-jellyseerr
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-jellyseerr
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.71
  selector:
    app.kubernetes.io/name: mcp-jellyseerr
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
