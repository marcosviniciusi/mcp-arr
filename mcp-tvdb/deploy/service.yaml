apiVersion: v1
kind: Service
metadata:
  name: mcp-tvdb
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-tvdb
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.70
  selector:
    app.kubernetes.io/name: mcp-tvdb
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
