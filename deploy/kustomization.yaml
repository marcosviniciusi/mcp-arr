apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ia-mcp

commonLabels:
  app.kubernetes.io/name: midia-mcp
  app.kubernetes.io/part-of: ia-mcp

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - sealedsecret-config.yaml
