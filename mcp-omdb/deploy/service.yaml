apiVersion: v1
kind: Service
metadata:
  name: mcp-omdb
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-omdb
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.59
  selector:
    app.kubernetes.io/name: mcp-omdb
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
