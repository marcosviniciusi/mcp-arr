apiVersion: v1
kind: Service
metadata:
  name: mcp-tmdb
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-tmdb
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.58
  selector:
    app.kubernetes.io/name: mcp-tmdb
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
