apiVersion: v1
kind: Service
metadata:
  name: mcp-whisparr
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-whisparr
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.69
  selector:
    app.kubernetes.io/name: mcp-whisparr
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
