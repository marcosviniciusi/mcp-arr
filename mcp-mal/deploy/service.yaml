apiVersion: v1
kind: Service
metadata:
  name: mcp-mal
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-mal
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.65
  selector:
    app.kubernetes.io/name: mcp-mal
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
