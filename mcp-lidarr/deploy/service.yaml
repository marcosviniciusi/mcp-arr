apiVersion: v1
kind: Service
metadata:
  name: mcp-lidarr
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-lidarr
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.68
  selector:
    app.kubernetes.io/name: mcp-lidarr
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
