apiVersion: v1
kind: Service
metadata:
  name: mcp-prowlarr
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-prowlarr
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.60
  selector:
    app.kubernetes.io/name: mcp-prowlarr
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
