apiVersion: v1
kind: Service
metadata:
  name: mcp-sonarr
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-sonarr
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.56
  selector:
    app.kubernetes.io/name: mcp-sonarr
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
