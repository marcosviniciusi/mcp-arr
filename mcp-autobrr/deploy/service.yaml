apiVersion: v1
kind: Service
metadata:
  name: mcp-autobrr
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-autobrr
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.61
  selector:
    app.kubernetes.io/name: mcp-autobrr
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
