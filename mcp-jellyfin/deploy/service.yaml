apiVersion: v1
kind: Service
metadata:
  name: mcp-jellyfin
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-jellyfin
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.64
  selector:
    app.kubernetes.io/name: mcp-jellyfin
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
