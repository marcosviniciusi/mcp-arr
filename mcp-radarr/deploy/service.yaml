apiVersion: v1
kind: Service
metadata:
  name: mcp-radarr
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-radarr
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.57
  selector:
    app.kubernetes.io/name: mcp-radarr
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
