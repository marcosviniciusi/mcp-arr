apiVersion: v1
kind: Service
metadata:
  name: mcp-bazarr
  namespace: mcp-arr
  labels:
    app.kubernetes.io/name: mcp-bazarr
    app.kubernetes.io/part-of: mcp-arr
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.62
  selector:
    app.kubernetes.io/name: mcp-bazarr
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
